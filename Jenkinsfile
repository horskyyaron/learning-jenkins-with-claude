pipeline {
    agent any

    stages {
        stage('Inspect') {
            steps {
                sh 'pwd'
                sh 'exit 1'
            }
        }

        stage('Test') {
            steps {
                sh 'echo No tests configured yet'
            }
        }
    }
}
