# Jenkinsfile Baseline

The workspace now contains a root-level `Jenkinsfile` with `agent any`, an `Inspect` stage, and a placeholder `Test` stage. Future lessons can assume the learner has been exposed to the basic Declarative Pipeline shape, the `sh` step, and the reason a `Jenkinsfile` lives with the project, but should still reinforce these ideas through practice.
