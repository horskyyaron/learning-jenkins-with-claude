# Local Jenkins Is Running

The learner has started Jenkins in a Docker container, unlocked it with the initial admin password, logged in, and installed the recommended plugins. Future lessons can move from installation concepts to using the Jenkins UI to create jobs and run the existing root-level `Jenkinsfile`.
