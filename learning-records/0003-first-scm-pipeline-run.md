# First SCM Pipeline Run

The learner created a GitHub repository for this project, pushed the workspace, configured Jenkins to read the repository `Jenkinsfile`, and completed a first Pipeline run with both stages executing `pwd` and `ls`. Future lessons can assume a working baseline SCM Pipeline exists and can focus on interpreting console output, changing the `Jenkinsfile`, and diagnosing controlled failures.
